using UniRx;
using UnityEngine;
using UnityEngine.UI;

public class UnkoDartsButtonView : MonoBehaviour
{
    [SerializeField] private Button myButton;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Awake()
    {
        //UniRxの機能で書くよ
        
    }
}
